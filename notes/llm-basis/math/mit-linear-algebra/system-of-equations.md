## 方程组的几何解释
- 矩阵形式。
  - 简化表示。
  - 容易手工求解。
- row picture 
  - 从"方程（行）"的角度出发，将每一个方程视为几何空间中的一个"几何形状"，而整个方程组的解就是这些几何形状的"交点"。
- column picture. **最重要的**
  - 左边看成几个向量的线性组合，每个向量就是等式对齐未知数的一列。
  - 右边也看成一个向量。
- 对于任意的b, 是否都有解?
  - $$\mathbf{A}\mathbf{x} = \mathbf{b}$$
  - 考虑三维空间，如果向量a, b，c属于同一个平面，那么它们的线性组合也只能在这个平面。此时，不可能组成任意的b。(b也是三维平面向量)。
    - 这时，这个方程组就没有解。
- 重新思考矩阵与向量相乘的计算方式。
  - $$\mathbf{A}\mathbf{x} = \mathbf{b}$$
  - 两种计算方式：
    - 看成行向量的点积。
    - 看成列向量的组合。**在线性代数的视角下，最好用这种，尤其是大矩阵。**

## 消元法
- 方程组的情况下，注重的是行向量(row operation)，因为行向量是有语义的。
- 有可能成功，也有可能不成功。
  - 不成功就是奇异矩阵。
- 消元法使得原始矩阵A变成上三角矩阵U。
- 这里介绍了matlab是怎么算方程组的。
- 这块主要是计算，跟以前的学习的差不多。
- **消元法对应的矩阵操作** 
  - 这个是以前没学过的视角。
  - 例如一个方程组，要从第二行里面减去第一行，消掉第二行的主元x。
  - 从矩阵操作的角度来考虑，这个问题是：要达到相同的效果，**在前面要做一个什么样的矩阵X**，使得:
  - X * A = U, U是一个第二行消元了x的矩阵。
- 所以消元法也可以表示成一系列的矩阵操作。
  - 基本就是在前面叠加矩阵乘法。
    - 这里用到了左乘矩阵的的行是怎么作用到方程组的每组的过程，这个熟练。
    - enrich: {举例子说明，包括置换矩阵，某个消元矩阵等等
  - 参与矩阵乘法的都是初等矩阵。
  - enrich: {html文档举一个消元法的矩阵乘法例子来说明}
  - 将所有的矩阵乘法合并成一个矩阵。
    - **注意，矩阵乘法不满足交换律，但是可以随便加括号，满足结合律**。
- **结合律很重要，在很多证明中都会用到。**
- 在理解了消元法对应的矩阵运算以后，对于这种消元法对应的矩阵(每一步是初等矩阵，但是最终矩阵是复合矩阵)。
  - 求解逆矩阵，可以通过消元动作反推，不需要按照运算法则来强行计算。
  - enrich: {举例说明}